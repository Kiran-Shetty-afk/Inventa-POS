spring:
  application:
    name: pos-system

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:pos}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Poonamdate19}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: kiranshetty2004@gmail.com
    password: dlyy jdie bzar sswq
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  devtools:
    livereload:
      enabled: true

server:
  port: 5000

razorpay:
  api:
    key: rzp_test_SbTRKjAd1YcufV
    secret: 3kFn0T2C9IQwp048Mz1dZpcy

stripe:
  api:
    key: ${STRIPE_SECRET:}

payments:
  stripe:
    enabled: false

app:
  frontend:
    reset-url: http://localhost:5173/auth/reset-password?token=
  seed:
    historical:
      enabled: false
      volume: small
      days: 90
      reset: false
      random: 42
      shifts: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql: TRACE
