# Base URL of the API Gateway (Port 5000)
# In dev mode, leave empty or unset to use Vite's built-in dev proxy.
# In production builds, set this to your gateway URL.
VITE_API_BASE_URL=http://localhost:5000
